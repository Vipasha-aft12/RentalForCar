'use client';
import { useEffect } from 'react';
const JS = "\n(function () {\n  'use strict';\n  document.body.classList.remove('no-js');\n\n  /* Split build: one route per file, so the router stands down and the\n     browser follows links. Trip details travel in sessionStorage. */\n  var MULTIPAGE = document.querySelectorAll('[data-route]').length === 1;\n  var PAGE_MAP = {\"/\": \"index-desktop.html\", \"/cars/\": \"cars-desktop.html\", \"/book/\": \"book-desktop.html\", \"/cars/economy/\": \"cars-economy-desktop.html\", \"/cars/compact/\": \"cars-compact-desktop.html\", \"/cars/suv/\": \"cars-suv-desktop.html\", \"/cars/luxury/\": \"cars-luxury-desktop.html\", \"/cars/electric/\": \"cars-electric-desktop.html\", \"/cars/vans/\": \"cars-vans-desktop.html\", \"/locations/\": \"locations-desktop.html\", \"/car-rental/new-york/\": \"car-rental-new-york-desktop.html\", \"/car-rental/miami/\": \"car-rental-miami-desktop.html\", \"/car-rental/orlando/\": \"car-rental-orlando-desktop.html\", \"/car-rental/los-angeles/\": \"car-rental-los-angeles-desktop.html\", \"/car-rental/las-vegas/\": \"car-rental-las-vegas-desktop.html\", \"/car-rental/chicago/\": \"car-rental-chicago-desktop.html\", \"/car-rental/jfk-airport/\": \"car-rental-jfk-airport-desktop.html\", \"/car-rental/lax-airport/\": \"car-rental-lax-airport-desktop.html\", \"/car-rental/miami-airport/\": \"car-rental-miami-airport-desktop.html\", \"/car-rental/orlando-airport/\": \"car-rental-orlando-airport-desktop.html\", \"/how-it-works/\": \"how-it-works-desktop.html\", \"/faq/\": \"faq-desktop.html\", \"/guides/\": \"guides-desktop.html\", \"/guides/airport-car-rental-guide/\": \"guides-airport-car-rental-guide-desktop.html\", \"/guides/car-rental-tips/\": \"guides-car-rental-tips-desktop.html\", \"/guides/best-car-rental-for-road-trips/\": \"guides-best-car-rental-for-road-trips-desktop.html\", \"/about/\": \"about-desktop.html\", \"/contact/\": \"contact-desktop.html\", \"/manage-booking/\": \"manage-booking-desktop.html\", \"/privacy/\": \"privacy-desktop.html\", \"/terms/\": \"terms-desktop.html\", \"/404/\": \"404-desktop.html\", \"/cars/toyota-corolla/\": \"cars-toyota-corolla-desktop.html\", \"/cars/toyota-corolla-hybrid/\": \"cars-toyota-corolla-hybrid-desktop.html\", \"/cars/honda-civic/\": \"cars-honda-civic-desktop.html\", \"/cars/volkswagen-golf/\": \"cars-volkswagen-golf-desktop.html\", \"/cars/toyota-rav4/\": \"cars-toyota-rav4-desktop.html\", \"/cars/chevrolet-traverse/\": \"cars-chevrolet-traverse-desktop.html\", \"/cars/mercedes-benz-e-class/\": \"cars-mercedes-benz-e-class-desktop.html\", \"/cars/tesla-model-3/\": \"cars-tesla-model-3-desktop.html\", \"/cars/hyundai-ioniq-5/\": \"cars-hyundai-ioniq-5-desktop.html\", \"/cars/chrysler-pacifica/\": \"cars-chrysler-pacifica-desktop.html\", \"/cars/rv-motorhome/\": \"cars-rv-motorhome-desktop.html\", \"/cars/camper-van/\": \"cars-camper-van-desktop.html\", \"/cars/passenger-van/\": \"cars-passenger-van-desktop.html\", \"/cars/pickup-truck/\": \"cars-pickup-truck-desktop.html\", \"/cars/moving-truck/\": \"cars-moving-truck-desktop.html\", \"/car-rental/phl-airport/\": \"car-rental-phl-airport-desktop.html\", \"/car-rental/lga-airport/\": \"car-rental-lga-airport-desktop.html\", \"/car-rental/dfw-airport/\": \"car-rental-dfw-airport-desktop.html\", \"/car-rental/iah-airport/\": \"car-rental-iah-airport-desktop.html\", \"/car-rental/jax-airport/\": \"car-rental-jax-airport-desktop.html\", \"/car-rental/phx-airport/\": \"car-rental-phx-airport-desktop.html\", \"/car-rental/atl-airport/\": \"car-rental-atl-airport-desktop.html\", \"/car-rental/sfo-airport/\": \"car-rental-sfo-airport-desktop.html\", \"/car-rental/sat-airport/\": \"car-rental-sat-airport-desktop.html\", \"/car-rental/sea-airport/\": \"car-rental-sea-airport-desktop.html\", \"/car-rental/ord-airport/\": \"car-rental-ord-airport-desktop.html\", \"/car-rental/aus-airport/\": \"car-rental-aus-airport-desktop.html\", \"/car-rental/bos-airport/\": \"car-rental-bos-airport-desktop.html\", \"/car-rental/sjc-airport/\": \"car-rental-sjc-airport-desktop.html\"};\n  function pageFor(r) { return PAGE_MAP[r] || null; }\n  function saveTrip(s) { try { sessionStorage.setItem('rfc-trip', JSON.stringify({ t: TRIP, s: s || 0 })); } catch (e) {} }\n\n  /* ----------------------------------------------------------\n     ROUTER\n     Every route already exists in the static HTML, so the crawler\n     and the Google Ads destination checker see full page copy with\n     scripting disabled. The router only toggles visibility and\n     swaps the metadata.\n\n     SERVER REQUIREMENT: rewrite all unknown paths to index.html\n     (Netlify: /* /index.html 200 \u2014 Nginx: try_files $uri /index.html)\n     so a direct hit on /car-rental/miami/ resolves.\n     ---------------------------------------------------------- */\n  var routes = {};\n  var nodes = document.querySelectorAll('[data-route]');\n  for (var i = 0; i < nodes.length; i++) routes[nodes[i].getAttribute('data-route')] = nodes[i];\n\n  var ORIGIN = 'https://www.rentalforcar.com';\n  var headMeta = {\n    desc: document.querySelector('meta[name=\"description\"]'),\n    canonical: document.querySelector('link[rel=\"canonical\"]'),\n    robots: document.querySelector('meta[name=\"robots\"]'),\n    ogTitle: document.querySelector('meta[property=\"og:title\"]'),\n    ogDesc: document.querySelector('meta[property=\"og:description\"]'),\n    ogUrl: document.querySelector('meta[property=\"og:url\"]'),\n    twTitle: document.querySelector('meta[name=\"twitter:title\"]'),\n    twDesc: document.querySelector('meta[name=\"twitter:description\"]')\n  };\n\n  function normalise(path) {\n    if (!path) return '/';\n    if (path.charAt(0) !== '/') path = '/' + path;\n    if (path.length > 1 && path.charAt(path.length - 1) !== '/') path += '/';\n    return path;\n  }\n\n  /* STANDALONE MODE\n     The file is opened directly (file://) or served as a single .html\n     rather than from a site root. Clean-path pushState is impossible\n     there, so routing falls back to the hash and the entire site still\n     works from one link. On a properly deployed root the clean URLs are\n     used exactly as before. */\n  var STANDALONE = location.protocol === 'file:' || /\\.html?$/i.test(location.pathname);\n\n  function currentPath() {\n    if (MULTIPAGE) return document.querySelector('[data-route]').getAttribute('data-route');\n    if (location.hash.indexOf('#/') === 0) return normalise(location.hash.slice(1));\n    if (STANDALONE) return '/';\n    return normalise(location.pathname);\n  }\n\n  /* Breadcrumb + FAQ structured data are rebuilt per route, and only\n     ever describe content that is actually visible on that route. */\n  function setJsonLd(id, data) {\n    var el = document.getElementById(id);\n    if (!data) { if (el) el.remove(); return; }\n    if (!el) { el = document.createElement('script'); el.type = 'application/ld+json'; el.id = id; document.head.appendChild(el); }\n    el.textContent = JSON.stringify(data);\n  }\n\n  function buildBreadcrumbs(view, path) {\n    var crumbNav = view.querySelector('.crumbs');\n    if (!crumbNav || path === '/') return null;\n    var items = [{ '@type': 'ListItem', position: 1, name: 'Home', item: ORIGIN + '/' }];\n    var links = crumbNav.querySelectorAll('a');\n    for (var i = 1; i < links.length; i++) {\n      items.push({ '@type': 'ListItem', position: i + 1, name: links[i].textContent.trim(), item: ORIGIN + links[i].getAttribute('href') });\n    }\n    var last = crumbNav.lastChild;\n    var leaf = (last && last.textContent ? last.textContent : '').trim();\n    if (leaf) items.push({ '@type': 'ListItem', position: items.length + 1, name: leaf, item: ORIGIN + path });\n    return { '@context': 'https://schema.org', '@type': 'BreadcrumbList', itemListElement: items };\n  }\n\n  function buildFaq(view) {\n    var blocks = view.querySelectorAll('.faq details');\n    if (!blocks.length) return null;\n    var qs = [];\n    for (var i = 0; i < blocks.length; i++) {\n      var q = blocks[i].querySelector('summary');\n      var a = blocks[i].querySelector('.faq-a');\n      if (!q || !a) continue;\n      qs.push({ '@type': 'Question', name: q.textContent.trim(), acceptedAnswer: { '@type': 'Answer', text: a.textContent.trim() } });\n    }\n    return qs.length ? { '@context': 'https://schema.org', '@type': 'FAQPage', mainEntity: qs } : null;\n  }\n\n  function render(path, push) {\n    path = normalise(path);\n    var view = routes[path] || routes['/404/'];\n    var actual = routes[path] ? path : '/404/';\n\n    for (var key in routes) routes[key].classList.remove('is-active');\n    view.classList.add('is-active');\n\n    var title = view.getAttribute('data-title') || 'RentalForCar';\n    var desc = view.getAttribute('data-desc') || '';\n    var noindex = view.getAttribute('data-noindex') === 'true';\n\n    document.title = title;\n    if (headMeta.desc) headMeta.desc.setAttribute('content', desc);\n    if (headMeta.canonical) headMeta.canonical.setAttribute('href', ORIGIN + actual);\n    if (headMeta.robots) headMeta.robots.setAttribute('content', noindex ? 'noindex,follow' : 'index,follow,max-image-preview:large');\n    if (headMeta.ogTitle) headMeta.ogTitle.setAttribute('content', title);\n    if (headMeta.ogDesc) headMeta.ogDesc.setAttribute('content', desc);\n    if (headMeta.ogUrl) headMeta.ogUrl.setAttribute('content', ORIGIN + actual);\n    if (headMeta.twTitle) headMeta.twTitle.setAttribute('content', title);\n    if (headMeta.twDesc) headMeta.twDesc.setAttribute('content', desc);\n\n    setJsonLd('ld-breadcrumb', buildBreadcrumbs(view, actual));\n    setJsonLd('ld-faq', buildFaq(view));\n\n    var navLinks = document.querySelectorAll('.nav-links a');\n    for (var n = 0; n < navLinks.length; n++) {\n      if (normalise(navLinks[n].getAttribute('href')) === actual) navLinks[n].setAttribute('aria-current', 'page');\n      else navLinks[n].removeAttribute('aria-current');\n    }\n\n    if (push && !STANDALONE) history.pushState({ path: actual }, '', path);\n    /* The second pass overrides the browser's own scroll restoration\n       after a hashchange, which otherwise lands mid-page. */\n    window.scrollTo(0, 0);\n    if (window.requestAnimationFrame) requestAnimationFrame(function () { window.scrollTo(0, 0); });\n    closeMenu();\n    observeReveals();\n    attachPhotos();\n    var sliders = view.querySelectorAll('[data-slider]');\n    for (var s = 0; s < sliders.length; s++) setSlide(sliders[s], 0);\n    var rails = view.querySelectorAll('[data-rail]');\n    for (var r2 = 0; r2 < rails.length; r2++) { rails[r2].scrollLeft = 0; syncRail(rails[r2]); }\n    if (actual === '/book/') { fillBookingFromTrip(); showStep(pendingStep || 1); pendingStep = 0; }\n    /* Move focus to the new page's heading so keyboard and screen-reader\n       users land where the content starts, not back at the top nav. */\n    var head = view.querySelector('h1');\n    if (head) { head.setAttribute('tabindex', '-1'); head.focus({ preventScroll: true }); }\n  }\n\n  document.addEventListener('click', function (e) {\n    var a = e.target.closest ? e.target.closest('a[data-link]') : null;\n    if (!a) return;\n    var href = a.getAttribute('href');\n    if (!href || href.charAt(0) !== '/') return;\n    if (e.metaKey || e.ctrlKey || e.shiftKey || e.button !== 0) return;\n    if (MULTIPAGE) return;\n    e.preventDefault();\n    if (STANDALONE) {\n      var target = normalise(href);\n      /* Changing the hash fires hashchange, which renders. If we are\n         already on that route, render directly so the click still works. */\n      if (normalise(location.hash.slice(1)) === target) render(target, false);\n      else location.hash = target;\n    } else {\n      render(href, true);\n    }\n  });\n\n  if (!MULTIPAGE) {\n    window.addEventListener('hashchange', function () { render(currentPath(), false); });\n    window.addEventListener('popstate', function () { render(currentPath(), false); });\n  }\n\n  /* ---------- MOBILE MENU ---------- */\n  var burger = document.querySelector('[data-burger]');\n  var panel = document.getElementById('mobile-panel');\n  function closeMenu() {\n    if (!burger || !panel) return;\n    burger.setAttribute('aria-expanded', 'false');\n    burger.setAttribute('aria-label', 'Open menu');\n    panel.classList.remove('is-open');\n  }\n  if (burger && panel) {\n    burger.addEventListener('click', function () {\n      var open = burger.getAttribute('aria-expanded') === 'true';\n      burger.setAttribute('aria-expanded', String(!open));\n      burger.setAttribute('aria-label', open ? 'Open menu' : 'Close menu');\n      panel.classList.toggle('is-open', !open);\n    });\n  }\n\n  document.addEventListener('keydown', function (e) {\n    if (e.key === 'Escape' && panel && panel.classList.contains('is-open')) {\n      closeMenu();\n      if (burger) burger.focus();\n    }\n  });\n\n  /* ----------------------------------------------------------\n     FAQ ON HOVER\n     Answers open as the pointer arrives and close as it leaves, so\n     scanning the list needs no clicking. Two things keep it from\n     being annoying: a short intent delay, so brushing past a row\n     doesn't flash it open, and click-to-pin, so an answer you\n     actually stopped to read stays put until you dismiss it.\n     Touch devices keep plain tap-to-open \u2014 there is no hover there,\n     and opening on tap is what a finger expects.\n     ---------------------------------------------------------- */\n  var canHover = window.matchMedia('(hover: hover) and (pointer: fine)').matches;\n  if (canHover) {\n    document.addEventListener('mouseover', function (e) {\n      var d = e.target.closest ? e.target.closest('.faq details') : null;\n      if (!d || d.dataset.pinned === '1') return;\n      clearTimeout(d._close);\n      d._open = setTimeout(function () { d.open = true; }, 90);\n    });\n\n    document.addEventListener('mouseout', function (e) {\n      var d = e.target.closest ? e.target.closest('.faq details') : null;\n      if (!d) return;\n      /* ignore movement between a summary and its own answer */\n      if (e.relatedTarget && d.contains(e.relatedTarget)) return;\n      clearTimeout(d._open);\n      if (d.dataset.pinned === '1') return;\n      d._close = setTimeout(function () { d.open = false; }, 220);\n    });\n\n    /* A click pins the answer open, so it survives the pointer leaving.\n       Clicking again unpins and closes it. */\n    document.addEventListener('click', function (e) {\n      var s = e.target.closest ? e.target.closest('.faq summary') : null;\n      if (!s) return;\n      var d = s.parentElement;\n      e.preventDefault();\n      if (d.dataset.pinned === '1') {\n        d.dataset.pinned = '0';\n        d.open = false;\n      } else {\n        d.dataset.pinned = '1';\n        d.open = true;\n      }\n    });\n  }\n\n  /* ----------------------------------------------------------\n     CURSOR GLOW\n     One delegated pointer listener and one animation frame loop for\n     the whole page, rather than a listener per component. The light\n     eases toward the pointer instead of snapping to it, which is what\n     stops it looking like a jumping dot on fast movements.\n     ---------------------------------------------------------- */\n  var GLOW_SEL = '.btn, a.card, .vcard, .faq details, .pill-links a, .chip';\n  if (window.matchMedia('(hover: hover) and (pointer: fine)').matches) {\n    var glowEl = null, glowLayer = null;\n    var tx = 0, ty = 0, gx = 0, gy = 0, frame = 0;\n    var smooth = !window.matchMedia('(prefers-reduced-motion: reduce)').matches;\n\n    function layerFor(el) {\n      var g = el.querySelector(':scope > .cursor-glow');\n      if (!g) {\n        g = document.createElement('span');\n        g.className = 'cursor-glow';\n        g.setAttribute('aria-hidden', 'true');\n        el.appendChild(g);\n      }\n      return g;\n    }\n\n    function tick() {\n      frame = 0;\n      if (!glowLayer) return;\n      /* ease toward the pointer; the closer it gets the slower it\n         moves, which is what makes it feel like light rather than a\n         cursor follower */\n      gx += (tx - gx) * (smooth ? 0.2 : 1);\n      gy += (ty - gy) * (smooth ? 0.2 : 1);\n      glowLayer.style.setProperty('--gx', gx.toFixed(1) + 'px');\n      glowLayer.style.setProperty('--gy', gy.toFixed(1) + 'px');\n      if (smooth && (Math.abs(tx - gx) > 0.4 || Math.abs(ty - gy) > 0.4)) {\n        frame = requestAnimationFrame(tick);\n      }\n    }\n\n    function pump() { if (!frame) frame = requestAnimationFrame(tick); }\n\n    function release() {\n      if (glowEl) glowEl.classList.remove('has-glow');\n      glowEl = null; glowLayer = null;\n      if (frame) { cancelAnimationFrame(frame); frame = 0; }\n    }\n\n    document.addEventListener('pointermove', function (e) {\n      if (e.pointerType === 'touch') return;\n      var el = e.target.closest ? e.target.closest(GLOW_SEL) : null;\n      if (el !== glowEl) {\n        release();\n        if (!el) return;\n        glowEl = el;\n        glowLayer = layerFor(el);\n        var r0 = el.getBoundingClientRect();\n        /* start the light already under the pointer, so entering an\n           element never drags a streak across it */\n        gx = tx = e.clientX - r0.left;\n        gy = ty = e.clientY - r0.top;\n        glowLayer.style.setProperty('--gx', gx.toFixed(1) + 'px');\n        glowLayer.style.setProperty('--gy', gy.toFixed(1) + 'px');\n        el.classList.add('has-glow');\n        return;\n      }\n      if (!glowEl) return;\n      var r = glowEl.getBoundingClientRect();\n      tx = e.clientX - r.left;\n      ty = e.clientY - r.top;\n      pump();\n    }, { passive: true });\n\n    /* leaving the window, or scrolling the element out from under the\n       pointer, both need to fade it out cleanly */\n    document.addEventListener('pointerleave', release);\n    window.addEventListener('blur', release);\n    document.addEventListener('pointerdown', function (e) {\n      if (e.pointerType === 'touch') release();\n    }, { passive: true });\n  }\n\n  /* ----------------------------------------------------------\n     CAR IMAGE SLIDER\n     One delegated handler for every slider on the site. Arrows, dots,\n     keyboard and swipe all drive the same setSlide, so there is one\n     place where the index is clamped and the dots are synced.\n     ---------------------------------------------------------- */\n  function setSlide(slider, index) {\n    var track = slider.querySelector('[data-slider-track]');\n    if (!track) return;\n    var count = track.children.length;\n    if (!count) return;\n    index = Math.max(0, Math.min(count - 1, index));\n    slider.dataset.index = index;\n    track.style.transform = 'translateX(' + (-index * 100) + '%)';\n    var dots = slider.querySelectorAll('[data-slide-to]');\n    for (var i = 0; i < dots.length; i++) {\n      if (i === index) dots[i].setAttribute('aria-current', 'true');\n      else dots[i].removeAttribute('aria-current');\n    }\n    var slides = track.children;\n    for (var s = 0; s < slides.length; s++) {\n      /* keep the off-screen slides out of the tab order */\n      slides[s].setAttribute('aria-hidden', s === index ? 'false' : 'true');\n    }\n  }\n\n  document.addEventListener('click', function (e) {\n    if (!e.target.closest) return;\n    var step = e.target.closest('[data-slide]');\n    if (step) {\n      var sl = step.closest('[data-slider]');\n      setSlide(sl, (parseInt(sl.dataset.index || 0, 10)) + parseInt(step.getAttribute('data-slide'), 10));\n      return;\n    }\n    var dot = e.target.closest('[data-slide-to]');\n    if (dot) setSlide(dot.closest('[data-slider]'), parseInt(dot.getAttribute('data-slide-to'), 10));\n  });\n\n  document.addEventListener('keydown', function (e) {\n    if (e.key !== 'ArrowLeft' && e.key !== 'ArrowRight') return;\n    var sl = e.target.closest ? e.target.closest('[data-slider]') : null;\n    if (!sl) return;\n    setSlide(sl, (parseInt(sl.dataset.index || 0, 10)) + (e.key === 'ArrowRight' ? 1 : -1));\n  });\n\n  /* swipe */\n  (function () {\n    var sx = 0, sy = 0, active = null;\n    document.addEventListener('touchstart', function (e) {\n      active = e.target.closest ? e.target.closest('[data-slider]') : null;\n      if (!active) return;\n      sx = e.touches[0].clientX; sy = e.touches[0].clientY;\n    }, { passive: true });\n    document.addEventListener('touchend', function (e) {\n      if (!active) return;\n      var dx = e.changedTouches[0].clientX - sx;\n      var dy = e.changedTouches[0].clientY - sy;\n      /* only act on a deliberate horizontal swipe, so scrolling the\n         page through a slider doesn't flick the image */\n      if (Math.abs(dx) > 45 && Math.abs(dx) > Math.abs(dy) * 1.5) {\n        setSlide(active, (parseInt(active.dataset.index || 0, 10)) + (dx < 0 ? 1 : -1));\n      }\n      active = null;\n    }, { passive: true });\n  })();\n\n  /* ---------- CATEGORY RAIL ---------- */\n  function railStep(rail) {\n    var card = rail.querySelector('.card');\n    return card ? card.getBoundingClientRect().width + 18 : 300;\n  }\n  function syncRail(rail) {\n    var wrap = rail.closest('.rail-wrap');\n    if (!wrap) return;\n    var prev = wrap.querySelector('.rail-nav.prev');\n    var next = wrap.querySelector('.rail-nav.next');\n    var max = rail.scrollWidth - rail.clientWidth;\n    if (prev) prev.hidden = rail.scrollLeft < 8;\n    if (next) next.hidden = rail.scrollLeft > max - 8;\n  }\n  document.addEventListener('click', function (e) {\n    var btn = e.target.closest ? e.target.closest('[data-rail-move]') : null;\n    if (!btn) return;\n    var rail = btn.closest('.rail-wrap').querySelector('[data-rail]');\n    if (!rail) return;\n    /* move by whole cards, so the rail never stops mid-card */\n    rail.scrollLeft += railStep(rail) * parseInt(btn.getAttribute('data-rail-move'), 10) * 2;\n  });\n  document.addEventListener('scroll', function (e) {\n    if (e.target && e.target.hasAttribute && e.target.hasAttribute('data-rail')) syncRail(e.target);\n  }, true);\n  window.addEventListener('resize', function () {\n    var rails = document.querySelectorAll('[data-rail]');\n    for (var i = 0; i < rails.length; i++) syncRail(rails[i]);\n  });\n\n  /* ----------------------------------------------------------\n     CALL MODAL\n     Shown once per session, on exit intent on a pointer device and\n     on a timer where there is no cursor to leave with. Opens for\n     anyone who asks for it via [data-open-call]. The context line\n     names the page's own city or vehicle so it does not read as a\n     generic interruption.\n     ---------------------------------------------------------- */\n  (function () {\n    var modal = document.querySelector('[data-call-modal]');\n    if (!modal) return;\n    var lastFocus = null, armed = false;\n\n    function contextLine() {\n      var view = document.querySelector('.route.is-active');\n      if (!view) return '';\n      var route = view.getAttribute('data-route') || '';\n      var h1 = view.querySelector('h1');\n      var name = h1 ? h1.textContent.trim() : '';\n      if (route.indexOf('/car-rental/') === 0) {\n        /* keep the page's own preposition: \"in Miami\", \"at JFK Airport\" */\n        var where = name.replace(/^Car rental\\s+/i, '');\n        return 'Live rental specialists can help with car rental ' + where + '.';\n      }\n      if (route.indexOf('/cars/') === 0 && route !== '/cars/') {\n        return 'Questions about the ' + name + '? A specialist can check availability with you.';\n      }\n      if (route === '/book/') return 'Prefer to finish this booking on the phone? We can take it from here.';\n      if (route === '/manage-booking/') return 'We can find your booking and make the change on the call.';\n      return 'Speak to a rental specialist about your booking.';\n    }\n\n    function open() {\n      if (modal.classList.contains('is-open')) return;\n      var line = modal.querySelector('[data-cm-context]');\n      if (line) line.textContent = contextLine();\n      lastFocus = document.activeElement;\n      modal.hidden = false;\n      document.body.style.overflow = 'hidden';\n      /* Next frame for two reasons: the transition needs a start state\n         to run from, and the dialog is visibility:hidden until the\n         class lands \u2014 a hidden element cannot take focus. */\n      requestAnimationFrame(function () {\n        modal.classList.add('is-open');\n        var first = modal.querySelector('[data-cm-close]');\n        if (first) first.focus();\n      });\n      try { sessionStorage.setItem('rfc-cm', '1'); } catch (e) {}\n      if (window.dataLayer) window.dataLayer.push({ event: 'call_modal_open' });\n    }\n\n    function close() {\n      if (!modal.classList.contains('is-open')) return;\n      modal.classList.remove('is-open');\n      document.body.style.overflow = '';\n      setTimeout(function () { modal.hidden = true; }, 280);\n      if (lastFocus && lastFocus.focus) lastFocus.focus();\n    }\n\n    /* Escape is bound to the document: focus can sit outside the\n       dialog if the user clicks the backdrop, and the key still has\n       to work from there. */\n    document.addEventListener('keydown', function (e) {\n      if (e.key === 'Escape' && modal.classList.contains('is-open')) close();\n    });\n\n    /* keep tabbing inside the dialog while it is open */\n    modal.addEventListener('keydown', function (e) {\n      if (e.key !== 'Tab') return;\n      var f = modal.querySelectorAll('a[href], button:not([disabled])');\n      if (!f.length) return;\n      var first = f[0], last = f[f.length - 1];\n      if (e.shiftKey && document.activeElement === first) { e.preventDefault(); last.focus(); }\n      else if (!e.shiftKey && document.activeElement === last) { e.preventDefault(); first.focus(); }\n    });\n\n    modal.addEventListener('click', function (e) {\n      if (e.target === modal || e.target.closest('[data-cm-close]')) close();\n      var opt = e.target.closest ? e.target.closest('[data-cm-intent]') : null;\n      if (opt && window.dataLayer) {\n        window.dataLayer.push({ event: 'call_click', intent: opt.getAttribute('data-cm-intent') });\n      }\n    });\n\n    document.addEventListener('click', function (e) {\n      if (e.target.closest && e.target.closest('[data-open-call]')) { e.preventDefault(); open(); }\n    });\n\n    /* Once per session only. Nobody needs this twice. */\n    var seen = false;\n    try { seen = sessionStorage.getItem('rfc-cm') === '1'; } catch (e) {}\n    if (seen) return;\n\n    setTimeout(function () { armed = true; }, 8000);   /* not straight away */\n\n    if (window.matchMedia('(hover: hover) and (pointer: fine)').matches) {\n      document.addEventListener('mouseout', function (e) {\n        if (armed && !e.relatedTarget && e.clientY <= 0) open();\n      });\n    } else {\n      /* no cursor to leave with, so use dwell plus depth instead */\n      var fired = false;\n      var check = function () {\n        if (fired || !armed) return;\n        var d = (window.scrollY + window.innerHeight) / document.body.scrollHeight;\n        if (d > 0.45) { fired = true; open(); }\n      };\n      window.addEventListener('scroll', check, { passive: true });\n      setTimeout(function () { if (!fired) { fired = true; open(); } }, 40000);\n    }\n  })();\n\n  /* ---------- SCROLL REVEAL ---------- */\n  var io = null;\n  if ('IntersectionObserver' in window && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {\n    io = new IntersectionObserver(function (entries) {\n      entries.forEach(function (entry) {\n        if (entry.isIntersecting) { entry.target.classList.add('in'); io.unobserve(entry.target); }\n      });\n    }, { rootMargin: '0px 0px -8% 0px', threshold: 0.08 });\n  }\n  function observeReveals() {\n    var items = document.querySelectorAll('.route.is-active .reveal:not(.in)');\n    for (var i = 0; i < items.length; i++) {\n      if (io) io.observe(items[i]); else items[i].classList.add('in');\n    }\n  }\n\n  /* ----------------------------------------------------------\n     CARD PHOTOGRAPHY\n     Every image box on the site looks for a photo in the img/\n     folder next to this page. Drop the files in and the whole\n     site switches from illustrations to photography \u2014 no code\n     changes. Missing files fail silently and keep the artwork.\n\n     The filename is worked out from what the box belongs to:\n       vehicle class  -> img/economy.jpg, img/suv.jpg ...\n       city page      -> img/miami.jpg, img/chicago.jpg ...\n       airport page   -> img/jfk-airport.jpg ...\n       guide          -> img/car-rental-tips.jpg ...\n     .webp is tried first, then .jpg, then .png.\n     ---------------------------------------------------------- */\n  var IMAGE_DIR = 'img/';\n  var IMAGE_EXT = ['webp', 'jpg', 'png'];\n\n  /* Real photography from the Unsplash CDN. Unsplash Licence: free for\n     commercial use, no attribution required, and serving straight from\n     their CDN is the delivery method they intend. Used only as the\n     fallback \u2014 anything you put in img/ wins, so replacing a photo is\n     just dropping a file in with the matching name. */\n  var STOCK = {\n    economy:   '1492144534655-ae79c964c9d7',\n    compact:   '1459603677915-a62079ffd002',\n    suv:       '1533473359331-0135ef1b58bf',\n    luxury:    '1580273916550-e323be2ae537',\n    electric:  '1704340142770-b52988e5b6eb',\n    vans:      '1519994007676-baabab4bf574',\n\n    'new-york':    '1555215695-3004980ad54e',\n    miami:         '1508974239320-0a029497e820',\n    orlando:       '1502877338535-766e1452684a',\n    'los-angeles': '1568605117036-5fe5e7bab0b7',\n    'las-vegas':   '1511919884226-fd3cad34687c',\n    chicago:       '1567808291548-fc3ee04dbcf0',\n\n    'jfk-airport':     '1494976388531-d1058494cdd8',\n    'lax-airport':     '1542362567-b07e54358753',\n    'miami-airport':   '1542282088-fe8426682b8f',\n    'orlando-airport': '1608341089966-92c09e62214f',\n\n\n    /* slider slides 2 and 3 \u2014 interiors and close details, same licence */\n    'economy-interior':  '1599912027667-755b68b4dd3b',\n    'compact-interior':  '1667893591090-19729ae8fee3',\n    'suv-interior':      '1615517331072-da8aab44f923',\n    'luxury-interior':   '1533630217389-3a5e4dff5683',\n    'electric-interior': '1634804658555-248d9e9a212f',\n    'vans-interior':     '1631035012469-751baca1d4a0',\n\n    'economy-detail':  '1517026575980-3e1e2dedeab4',\n    'compact-detail':  '1597386601945-8980df52c3dc',\n    'suv-detail':      '1585014165903-6d6c6ebad3e9',\n    'luxury-detail':   '1611760357505-922600d8ffa6',\n    'electric-detail': '1610913721979-b20ede600e63',\n    'vans-detail':     '1511697344731-2419473a40c8',\n    'airport-car-rental-guide':      '1482029255085-35a4a48b7084',\n    'car-rental-tips':               '1498887960847-2a5e46312788',\n    'best-car-rental-for-road-trips': '1603584173870-7f23fdae1b7a'\n  };\n\n  function stockUrl(slug, w) {\n    var id = STOCK[slug];\n    return id ? 'https://images.unsplash.com/photo-' + id +\n      '?auto=format&fit=crop&w=' + w + '&q=70' : '';\n  }\n\n  function tailOf(path) {\n    return String(path || '').split('?')[0].replace(/\\/+$/, '').split('/').pop();\n  }\n\n  /* the fleet page tags a minivan as \"van\", the category page calls the\n     same thing \"vans\" \u2014 one file serves both */\n  var SLUG_ALIAS = { van: 'vans' };\n\n  function mediaSlug(box) {\n    /* Detail pages say which class image they want, since their own\n       URL slug is the model name and no photo is filed under that. */\n    var explicit = box.getAttribute('data-img');\n    if (explicit) return explicit;\n    var card = box.closest ? box.closest('.vcard') : null;\n    if (card) {\n      var t = card.getAttribute('data-type');\n      if (t) return SLUG_ALIAS[t] || t;\n      var cl = card.querySelector('a[href^=\"/cars/\"]');\n      if (cl) return tailOf(cl.getAttribute('href'));\n    }\n    var link = box.closest ? box.closest('a[href^=\"/\"]') : null;\n    if (link) return tailOf(link.getAttribute('href'));\n    var view = box.closest ? box.closest('[data-route]') : null;\n    if (view) return tailOf(view.getAttribute('data-route'));\n    return '';\n  }\n\n  function altFor(box, slug) {\n    var card = box.closest ? box.closest('.card, .vcard, .route') : null;\n    var head = card ? card.querySelector('h1, h2, h3') : null;\n    var text = head ? head.textContent.trim() : slug.replace(/-/g, ' ');\n    return text + ' \u2014 RentalForCar';\n  }\n\n  function attachPhotos() {\n    var boxes = document.querySelectorAll('.media:not([data-photo-done])');\n    for (var i = 0; i < boxes.length; i++) {\n      (function (box) {\n        box.setAttribute('data-photo-done', '1');\n        var slug = mediaSlug(box);\n        if (!slug || slug === 'book' || slug === 'cars') return;\n        var wide = box.classList.contains('media-wide');\n        var srcs = [];\n        for (var e = 0; e < IMAGE_EXT.length; e++) srcs.push(IMAGE_DIR + slug + '.' + IMAGE_EXT[e]);\n        var stock = stockUrl(slug, wide ? 1600 : 900);\n        if (stock) srcs.push(stock);\n        if (!srcs.length) return;\n\n        var n = 0;\n        var img = document.createElement('img');\n        img.alt = altFor(box, slug);\n        img.loading = 'lazy';\n        img.decoding = 'async';\n        img.addEventListener('load', function () { img.classList.add('is-loaded'); });\n        img.addEventListener('error', function () {\n          n++;\n          if (n < srcs.length) img.src = srcs[n];\n          else img.remove();\n        });\n        img.src = srcs[0];\n        box.insertBefore(img, box.firstChild);\n      })(boxes[i]);\n    }\n  }\n\n  /* ----------------------------------------------------------\n     HERO VIDEO\n     mp4 is the footage: cars moving on a highway, from Pexels, free\n     for commercial use with no attribution required. still is the\n     matching frame from the same clip \u2014 it shows while the video\n     buffers and stands in if the video cannot load.\n\n     There is no drawn animation behind this any more: the banner is\n     the footage, with the photograph beneath it. Self-host both\n     before launch so the banner doesn't depend on their CDN.\n     ---------------------------------------------------------- */\n  var HERO_VIDEO = {\n    mp4: 'https://videos.pexels.com/video-files/854671/854671-hd_1920_1080_25fps.mp4',\n    still: 'https://images.pexels.com/videos/854671/free-video-854671.jpg?auto=compress&cs=tinysrgb&w=1600'\n  };\n\n  /* Autoplay gets refused in a few real situations: iOS Low Power Mode,\n     a sandboxed preview frame, some in-app browsers. Catch the refusal\n     and start on the visitor's first interaction instead. */\n  function attemptPlay(v) {\n    var go = v.play();\n    if (!go || !go.catch) return;\n    go.catch(function () {\n      var events = ['touchstart', 'pointerdown', 'click', 'keydown', 'scroll'];\n      var kick = function () { v.play().then(cleanup, function () {}); };\n      var cleanup = function () {\n        events.forEach(function (e) { document.removeEventListener(e, kick, true); });\n      };\n      events.forEach(function (e) {\n        document.addEventListener(e, kick, { capture: true, passive: true });\n      });\n    });\n  }\n\n  function loadHeroVideo() {\n    var art = document.querySelector('.hero-art');\n    var still = document.querySelector('[data-hero-still]');\n    var v = document.querySelector('[data-hero-video]');\n    if (!art) return;\n\n    if (still && HERO_VIDEO.still) {\n      still.addEventListener('load', function () { art.classList.add('has-still'); });\n      still.src = HERO_VIDEO.still;\n    }\n\n    if (!v || !HERO_VIDEO.mp4) return;\n    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;\n    var conn = navigator.connection;\n    if (conn && (conn.saveData || /(^|-)2g$/.test(conn.effectiveType || ''))) return;\n\n    var show = function () { if (v.readyState >= 2) art.classList.add('has-video'); };\n    ['loadeddata', 'canplay', 'playing', 'timeupdate'].forEach(function (e) {\n      v.addEventListener(e, show);\n    });\n    /* If the footage cannot load, the photograph stays. */\n    v.addEventListener('error', function () { art.classList.remove('has-video'); }, true);\n\n    v.setAttribute('autoplay', '');\n    v.setAttribute('muted', '');\n    v.muted = true;\n    v.setAttribute('poster', HERO_VIDEO.still);\n    v.src = HERO_VIDEO.mp4;\n    v.load();\n    attemptPlay(v);\n  }\n\n  if (document.readyState === 'complete') setTimeout(loadHeroVideo, 400);\n  else window.addEventListener('load', function () { setTimeout(loadHeroVideo, 400); });\n\n  /* ---------- STICKY MOBILE CTA ---------- */\n  var sticky = document.querySelector('[data-sticky]');\n  if (sticky) {\n    var onScroll = function () { sticky.classList.toggle('is-shown', window.scrollY > 620); };\n    window.addEventListener('scroll', onScroll, { passive: true });\n    onScroll();\n  }\n\n  /* ---------- DEFAULT DATES ---------- */\n  function iso(d) { return d.toISOString().slice(0, 10); }\n  var today = new Date();\n  var pick = new Date(today.getTime() + 86400000);\n  var drop = new Date(today.getTime() + 86400000 * 4);\n  var fromEl = document.getElementById('h-from');\n  var toEl = document.getElementById('h-to');\n  if (fromEl) { fromEl.value = iso(pick); fromEl.min = iso(today); }\n  if (toEl) { toEl.value = iso(drop); toEl.min = iso(pick); }\n  if (fromEl && toEl) fromEl.addEventListener('change', function () { toEl.min = fromEl.value; if (toEl.value < fromEl.value) toEl.value = fromEl.value; });\n\n  /* ----------------------------------------------------------\n     TRIP STATE\n     What the visitor has told us so far, carried across the search\n     box, the fleet page and every booking step. Re-typing details\n     you already gave is the most common thing that makes a booking\n     flow feel stupid, so nothing is asked twice.\n     ---------------------------------------------------------- */\n  var TRIP = { pickup: '', from: '', to: '', age: '', flight: '', vehicle: '' };\n\n  function fmtDate(v) {\n    if (!v) return '';\n    var d = new Date(v);\n    if (isNaN(d)) return v;\n    return d.toLocaleDateString(undefined, { day: 'numeric', month: 'short' });\n  }\n\n  function renderTrip() {\n    var bar = document.querySelector('[data-trip]');\n    if (!bar) return;\n    var bits = [];\n    if (TRIP.pickup) bits.push('<b>' + TRIP.pickup.replace(/[<>]/g, '') + '</b>');\n    if (TRIP.from) bits.push(fmtDate(TRIP.from) + (TRIP.to ? ' \u2013 ' + fmtDate(TRIP.to) : ''));\n    if (TRIP.vehicle) bits.push(TRIP.vehicle);\n    if (TRIP.age) bits.push('Driver ' + TRIP.age);\n    if (!bits.length) { bar.hidden = true; return; }\n    bar.hidden = false;\n    bar.innerHTML = bits.join(' <span class=\"sep\">\u00b7</span> ') +\n      '<button type=\"button\" data-edit-trip>Edit trip</button>';\n    var f = document.querySelector('[data-summary-final]');\n    if (f) f.innerHTML = '<p class=\"muted\" style=\"margin:0\">' + bits.join(' \u00b7 ') + '</p>';\n  }\n\n  function fillBookingFromTrip() {\n    var set = function (id, val) { var el = document.getElementById(id); if (el && val) el.value = val; };\n    set('b-pickup', TRIP.pickup);\n    set('b-from', TRIP.from);\n    set('b-to', TRIP.to);\n    set('b-flight', TRIP.flight);\n    if (TRIP.age) {\n      var sel = document.getElementById('b-age');\n      if (sel) for (var i = 0; i < sel.options.length; i++) {\n        if (sel.options[i].text.indexOf(TRIP.age.slice(0, 2)) === 0) sel.selectedIndex = i;\n      }\n    }\n    if (TRIP.vehicle) {\n      var list = document.querySelector('[data-step=\"2\"] .grid');\n      var labels = document.querySelectorAll('[data-step=\"2\"] label.vcard');\n      var matched = false;\n      for (var k = 0; k < labels.length; k++) {\n        var head = labels[k].querySelector('h3');\n        if (head && head.textContent.trim().indexOf(TRIP.vehicle) === 0) {\n          var r = labels[k].querySelector('input[type=radio]');\n          if (r) { r.checked = true; matched = true; }\n        }\n      }\n      /* The car you chose isn't one of the three defaults \u2014 add it at\n         the top and select it, rather than silently handing you a\n         different vehicle. */\n      if (!matched && list) {\n        var old = list.querySelector('[data-injected]');\n        if (old) old.remove();\n        var specs = (TRIP.specs || '').split('|').filter(Boolean)\n          .map(function (s) { return '<li>' + s + '</li>'; }).join('');\n        var lab = document.createElement('label');\n        lab.className = 'vcard';\n        lab.setAttribute('data-injected', '1');\n        lab.style.cssText = 'cursor:pointer;display:block;padding:18px';\n        lab.innerHTML =\n          '<div style=\"display:flex;gap:14px;align-items:flex-start\">' +\n          '<input type=\"radio\" name=\"vehicle\" style=\"width:20px;height:20px;margin-top:4px;accent-color:#101820\" checked>' +\n          '<div style=\"flex:1\"><h3 class=\"h-3\">' + TRIP.vehicle +\n          ' <span class=\"or-similar\">' + (TRIP.similar || 'or similar') + '</span></h3>' +\n          (specs ? '<ul class=\"spec-list\" style=\"margin-top:10px\">' + specs + '</ul>' : '') +\n          '</div></div>';\n        list.insertBefore(lab, list.firstChild);\n        var others = list.querySelectorAll('label:not([data-injected]) input[type=radio]');\n        for (var o = 0; o < others.length; o++) others[o].checked = false;\n      }\n    }\n    renderTrip();\n  }\n\n  var searchForm = document.querySelector('[data-search-form]');\n  if (searchForm) {\n    searchForm.addEventListener('submit', function (e) {\n      e.preventDefault();\n      /* Wire to the supplier availability API here, then render results\n         into /cars/ before routing. */\n      TRIP.pickup = (document.getElementById('h-pickup') || {}).value || '';\n      TRIP.from = (document.getElementById('h-from') || {}).value || '';\n      TRIP.to = (document.getElementById('h-to') || {}).value || '';\n      TRIP.age = (document.getElementById('h-age') || {}).value || '';\n      goBook(TRIP.pickup && TRIP.from && TRIP.to ? 2 : 1);\n    });\n  }\n\n  var pendingStep = 0;\n  function goBook(step) {\n    var target = step || 1;\n    pendingStep = target;\n    /* Capture the target: the router consumes pendingStep when it swaps\n       the view, so reading it again here would always come back as 1. */\n    var apply = function () { fillBookingFromTrip(); showStep(target); pendingStep = 0; };\n    if (MULTIPAGE) {\n      saveTrip(target);\n      var to = pageFor('/book/');\n      if (to && location.pathname.indexOf(to) === -1) { location.href = to; return; }\n      apply(); return;\n    }\n    if (STANDALONE) {\n      if (normalise(location.hash.slice(1)) !== '/book/') location.hash = '/book/';\n    } else if (normalise(location.pathname) !== '/book/') {\n      render('/book/', true);\n    }\n    /* Always deferred. A link may have started the navigation a moment\n       earlier, and applying synchronously would land before the router\n       swaps the view \u2014 which would then reset the step back to one. */\n    setTimeout(apply, 0);\n  }\n\n  /* Picking a car anywhere on the site carries that choice into the\n     booking rather than dropping you at a blank step 2. */\n  /* Card click routes to the detail page \u2014 unless the click landed on\n     something interactive, which keeps its own behaviour. */\n  document.addEventListener('click', function (e) {\n    if (!e.target.closest) return;\n    if (e.target.closest('a, button, input, label, select')) return;\n    var card = e.target.closest('.vcard[data-detail]');\n    if (!card) return;\n    var to = card.getAttribute('data-detail');\n    if (MULTIPAGE) { var f = pageFor(to); if (f) location.href = f; return; }\n    if (STANDALONE) location.hash = to; else render(to, true);\n  });\n\n  /* ----------------------------------------------------------\n     BOOK FROM A DETAIL PAGE\n     The form on a car page already knows the vehicle, so filling it in\n     answers both step one and step two of the booking. Validate it,\n     carry it into the trip, then open the flow at extras rather than\n     making the visitor retype what they just entered.\n     ---------------------------------------------------------- */\n  /* ----------------------------------------------------------\n     BOOK FROM A LOCATION PAGE\n     The city and airport pages know the pick-up point but not the\n     vehicle, so this fills in step one and opens the flow at the car\n     choice rather than at a blank form.\n     ---------------------------------------------------------- */\n  document.addEventListener('click', function (e) {\n    var go = e.target.closest ? e.target.closest('[data-location-book-go]') : null;\n    if (!go) return;\n    var card = go.closest('[data-location-book]');\n    if (!card) return;\n\n    var groups = card.querySelectorAll('[data-validate]');\n    var firstBad = null;\n    for (var i = 0; i < groups.length; i++) {\n      var input = groups[i].querySelector('input, select');\n      if (!input) continue;\n      var ok = String(input.value).trim() !== '';\n      if (ok && /-to$/.test(input.id)) {\n        var from = card.querySelector('[id$=\"-from\"]');\n        if (from && from.value && input.value <= from.value) ok = false;\n      }\n      groups[i].classList.toggle('has-error', !ok);\n      if (!ok && !firstBad) firstBad = input;\n    }\n    if (firstBad) { firstBad.focus(); return; }\n\n    var val = function (suffix) {\n      var el = card.querySelector('[id$=\"' + suffix + '\"]');\n      return el ? el.value : '';\n    };\n    TRIP.pickup = val('-pickup');\n    TRIP.from = val('-from');\n    TRIP.to = val('-to');\n    TRIP.age = val('-age');\n    TRIP.flight = val('-flight');\n    /* no car chosen here, so clear any carried over from a car page */\n    TRIP.vehicle = ''; TRIP.similar = ''; TRIP.specs = '';\n    goBook(2);\n  });\n\n  document.addEventListener('click', function (e) {\n    var go = e.target.closest ? e.target.closest('[data-detail-book-go]') : null;\n    if (!go) return;\n    var card = go.closest('[data-detail-book]');\n    if (!card) return;\n\n    var groups = card.querySelectorAll('[data-validate]');\n    var firstBad = null;\n    for (var i = 0; i < groups.length; i++) {\n      var input = groups[i].querySelector('input, select');\n      if (!input) continue;\n      var ok = String(input.value).trim() !== '';\n      if (ok && /-to$/.test(input.id)) {\n        var from = card.querySelector('[id$=\"-from\"]');\n        if (from && from.value && input.value <= from.value) ok = false;\n      }\n      groups[i].classList.toggle('has-error', !ok);\n      if (!ok && !firstBad) firstBad = input;\n    }\n    if (firstBad) { firstBad.focus(); return; }\n\n    var val = function (suffix) {\n      var el = card.querySelector('[id$=\"' + suffix + '\"]');\n      return el ? el.value : '';\n    };\n    TRIP.pickup = val('-pickup');\n    TRIP.from = val('-from');\n    TRIP.to = val('-to');\n    TRIP.age = val('-age');\n    TRIP.flight = val('-flight');\n    TRIP.vehicle = card.getAttribute('data-vehicle') || '';\n    TRIP.similar = card.getAttribute('data-similar') || '';\n    TRIP.specs = card.getAttribute('data-specs') || '';\n    goBook(3);\n  });\n\n  document.addEventListener('click', function (e) {\n    var pick = e.target.closest ? e.target.closest('[data-pick-vehicle]') : null;\n    if (!pick) return;\n    e.preventDefault();\n    TRIP.vehicle = pick.getAttribute('data-pick-vehicle');\n    TRIP.similar = pick.getAttribute('data-pick-similar') || '';\n    TRIP.specs = pick.getAttribute('data-pick-specs') || '';\n    goBook(2);\n  });\n\n  document.addEventListener('click', function (e) {\n    var edit = e.target.closest ? e.target.closest('[data-edit-trip]') : null;\n    if (edit) showStep(1);\n  });\n\n  /* ---------- FLEET FILTERS ---------- */\n  var fleet = document.querySelector('[data-fleet]');\n  if (fleet) {\n    var cards = fleet.querySelectorAll('.vcard');\n    var boxes = document.querySelectorAll('[data-filter]');\n    var countEl = document.querySelector('[data-result-count]');\n    var emptyEl = document.querySelector('[data-no-results]');\n\n    function applyFilters() {\n      var active = {};\n      for (var b = 0; b < boxes.length; b++) {\n        if (!boxes[b].checked) continue;\n        var g = boxes[b].getAttribute('data-filter');\n        (active[g] = active[g] || []).push(boxes[b].value);\n      }\n      var shown = 0;\n      for (var c = 0; c < cards.length; c++) {\n        var ok = true;\n        for (var group in active) {\n          var val = cards[c].getAttribute('data-' + group) || '';\n          if (active[group].indexOf(val) === -1) { ok = false; break; }\n        }\n        cards[c].hidden = !ok;\n        if (ok) shown++;\n      }\n      if (countEl) countEl.textContent = shown;\n      if (emptyEl) emptyEl.hidden = shown !== 0;\n    }\n    for (var b2 = 0; b2 < boxes.length; b2++) boxes[b2].addEventListener('change', applyFilters);\n\n    var clears = document.querySelectorAll('[data-clear-filters]');\n    for (var cl = 0; cl < clears.length; cl++) {\n      clears[cl].addEventListener('click', function () {\n        for (var b3 = 0; b3 < boxes.length; b3++) boxes[b3].checked = false;\n        applyFilters();\n      });\n    }\n    /* Show what is filtered and let it be undone in one tap \u2014 a\n       sidebar of ticked boxes you have to scroll back to is the usual\n       way people lose track of why results vanished. */\n    var chipBar = document.querySelector('[data-active-filters]');\n    var countEl2 = document.querySelector('[data-filter-count]');\n    function renderChips() {\n      if (!chipBar) return;\n      chipBar.innerHTML = '';\n      var on = 0;\n      for (var b = 0; b < boxes.length; b++) {\n        if (!boxes[b].checked) continue;\n        on++;\n        var label = boxes[b].parentElement.textContent.trim();\n        var chip = document.createElement('span');\n        chip.className = 'chip';\n        chip.textContent = label;\n        var x = document.createElement('button');\n        x.type = 'button'; x.innerHTML = '&times;';\n        x.setAttribute('aria-label', 'Remove ' + label + ' filter');\n        (function (box) {\n          x.addEventListener('click', function () { box.checked = false; applyFilters(); renderChips(); });\n        })(boxes[b]);\n        chip.appendChild(x);\n        chipBar.appendChild(chip);\n      }\n      if (on) {\n        var clear = document.createElement('button');\n        clear.type = 'button'; clear.className = 'btn btn-ghost btn-sm';\n        clear.textContent = 'Clear all';\n        clear.addEventListener('click', function () {\n          for (var b2 = 0; b2 < boxes.length; b2++) boxes[b2].checked = false;\n          applyFilters(); renderChips();\n        });\n        chipBar.appendChild(clear);\n      }\n      if (countEl2) { countEl2.hidden = !on; countEl2.textContent = on; }\n    }\n    for (var b4 = 0; b4 < boxes.length; b4++) boxes[b4].addEventListener('change', renderChips);\n    for (var cl2 = 0; cl2 < clears.length; cl2++) clears[cl2].addEventListener('click', renderChips);\n\n    var ft = document.querySelector('[data-filter-toggle]');\n    var fl = document.getElementById('filters');\n    if (ft && fl) ft.addEventListener('click', function () {\n      var open = fl.classList.toggle('is-open');\n      ft.setAttribute('aria-expanded', String(open));\n    });\n  }\n\n  /* ---------- BOOKING FLOW ---------- */\n  var stepLabels = { 1: 'Your trip', 2: 'Your car', 3: 'Protection and extras', 4: 'Driver details' };\n  function showStep(n) {\n    var panels = document.querySelectorAll('[data-step]');\n    if (!panels.length) return;\n    for (var i = 0; i < panels.length; i++) panels[i].hidden = panels[i].getAttribute('data-step') !== String(n);\n    var fill = document.querySelector('[data-progress]');\n    var label = document.querySelector('[data-step-label]');\n    var remain = document.querySelector('[data-step-remaining]');\n    var prog = document.querySelector('.progress');\n    var done = n > 4;\n    if (prog) prog.hidden = done;\n    if (fill) fill.style.width = (done ? 100 : n * 25) + '%';\n    if (label && !done) label.textContent = 'Step ' + n + ' of 4 \u2014 ' + stepLabels[n];\n    if (remain && !done) remain.textContent = (4 - n) === 0 ? 'Last step' : (4 - n) + ' step' + ((4 - n) > 1 ? 's' : '') + ' remaining';\n    var head = document.querySelector('.route.is-active .progress');\n    if (head) head.scrollIntoView({ behavior: 'smooth', block: 'start' });\n  }\n  function validateStep(n) {\n    var panel = document.querySelector('[data-step=\"' + n + '\"]');\n    if (!panel) return true;\n    var groups = panel.querySelectorAll('[data-validate]');\n    var firstBad = null;\n    for (var i = 0; i < groups.length; i++) {\n      var input = groups[i].querySelector('input, select');\n      if (!input) continue;\n      var ok = input.type === 'checkbox' ? input.checked : String(input.value).trim() !== '';\n      if (ok && input.type === 'email') ok = /^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$/.test(input.value);\n      if (ok && input.id === 'b-to') {\n        var from = document.getElementById('b-from');\n        if (from && from.value && input.value <= from.value) ok = false;\n      }\n      groups[i].classList.toggle('has-error', !ok);\n      if (!ok && !firstBad) firstBad = input;\n    }\n    if (firstBad) { firstBad.focus(); return false; }\n    return true;\n  }\n\n  function captureStep1() {\n    var g = function (id) { var el = document.getElementById(id); return el ? el.value : ''; };\n    TRIP.pickup = g('b-pickup'); TRIP.from = g('b-from'); TRIP.to = g('b-to');\n    TRIP.age = g('b-age'); TRIP.flight = g('b-flight');\n  }\n\n  function captureStep2() {\n    var checked = document.querySelector('[data-step=\"2\"] input[type=radio]:checked');\n    if (!checked) return;\n    var head = checked.closest('label').querySelector('h3');\n    if (head) TRIP.vehicle = head.childNodes[0].textContent.trim();\n  }\n\n  document.addEventListener('click', function (e) {\n    var btn = e.target.closest ? e.target.closest('[data-next]') : null;\n    if (!btn) return;\n    var target = parseInt(btn.getAttribute('data-next'), 10);\n    var current = parseInt(btn.closest('[data-step]').getAttribute('data-step'), 10);\n    /* Going back never blocks; going forward checks the step first. */\n    if (target > current && !validateStep(current)) return;\n    if (current === 1) captureStep1();\n    if (current === 2) captureStep2();\n    renderTrip();\n    showStep(target);\n  });\n\n  document.addEventListener('click', function (e) {\n    var go = e.target.closest ? e.target.closest('[data-confirm]') : null;\n    if (!go) return;\n    if (!validateStep(4)) return;\n    var ref = 'RFC-' + String(1000 + Math.floor(Math.random() * 8999)) +\n      '-' + 'ABCDEFGHJKLMNPQRSTUVWXYZ'.charAt(Math.floor(Math.random() * 24)) +\n      'ABCDEFGHJKLMNPQRSTUVWXYZ'.charAt(Math.floor(Math.random() * 24));\n    var el = document.querySelector('[data-ref]');\n    if (el) el.textContent = ref;\n    buildSummary(ref);\n    renderTrip();\n    showStep(5);\n  });\n\n  /* ----------------------------------------------------------\n     CONFIRMATION SUMMARY\n     Everything shown here is read back from what was actually\n     entered \u2014 nothing is invented. Amounts are deliberately absent:\n     the deposit and service fee come from the supplier response and\n     belong on the emailed confirmation, not in placeholder figures.\n     ---------------------------------------------------------- */\n  function longDate(v) {\n    if (!v) return '';\n    var d = new Date(v);\n    if (isNaN(d)) return v;\n    return d.toLocaleDateString(undefined, { weekday: 'short', day: 'numeric', month: 'long', year: 'numeric' }) +\n      ', ' + d.toLocaleTimeString(undefined, { hour: '2-digit', minute: '2-digit' });\n  }\n\n  function buildSummary(ref) {\n    var rows = [];\n    var esc = function (s) { return String(s || '').replace(/[<>&]/g, ''); };\n\n    if (TRIP.pickup) rows.push(['Pick-up location', esc(TRIP.pickup)]);\n    if (TRIP.from) rows.push(['Pick-up', longDate(TRIP.from)]);\n    if (TRIP.to) rows.push(['Return', longDate(TRIP.to)]);\n\n    if (TRIP.from && TRIP.to) {\n      var days = Math.max(1, Math.round((new Date(TRIP.to) - new Date(TRIP.from)) / 86400000));\n      rows.push(['Duration', days + (days === 1 ? ' day' : ' days')]);\n    }\n\n    /* the vehicle as it was actually selected on step 2 */\n    var picked = document.querySelector('[data-step=\"2\"] input[type=radio]:checked');\n    if (picked) {\n      var head = picked.closest('label').querySelector('h3');\n      var name = head ? head.childNodes[0].textContent.trim() : '';\n      var sim = head && head.querySelector('.or-similar') ? head.querySelector('.or-similar').textContent.trim() : '';\n      if (name) rows.push(['Vehicle', esc(name) + (sim ? ' <span class=\"or-similar\">' + esc(sim) + '</span>' : '')]);\n      var pills = picked.closest('label').querySelectorAll('.spec-list li');\n      if (pills.length) {\n        var spec = [];\n        for (var s = 0; s < pills.length; s++) spec.push(esc(pills[s].textContent));\n        rows.push(['Specification', spec.join(' \u00b7 ')]);\n      }\n    }\n\n    if (TRIP.age) rows.push(['Driver age', esc(TRIP.age)]);\n    if (TRIP.flight) rows.push(['Flight', esc(TRIP.flight)]);\n\n    /* extras ticked on step 3 */\n    var extras = [], boxes = document.querySelectorAll('[data-step=\"3\"] .check input:checked');\n    var hasDriver = false;\n    for (var e = 0; e < boxes.length; e++) {\n      var b = boxes[e].closest('.check').querySelector('b');\n      if (b) {\n        extras.push(esc(b.textContent));\n        if (/additional driver/i.test(b.textContent)) hasDriver = true;\n      }\n    }\n    rows.push(['Extras', extras.length ? extras.join(', ') : 'None added']);\n    rows.push(['Booked by', esc((document.getElementById('b-first') || {}).value + ' ' + (document.getElementById('b-last') || {}).value)]);\n    rows.push(['Reference', ref]);\n\n    var body = document.querySelector('[data-sum-table]');\n    if (body) {\n      var out = '';\n      for (var r = 0; r < rows.length; r++) {\n        out += '<tr><th scope=\"row\">' + rows[r][0] + '</th><td>' + rows[r][1] + '</td></tr>';\n      }\n      body.innerHTML = out;\n    }\n\n    var mail = document.querySelector('[data-sum-email]');\n    var email = (document.getElementById('b-email') || {}).value;\n    if (mail && email) mail.textContent = email;\n\n    var ad = document.querySelector('[data-sum-extra-drivers]');\n    if (ad) ad.hidden = !hasDriver;\n\n    CAL_REF = ref;\n  }\n\n  var CAL_REF = '';\n\n  /* A calendar file built in the browser \u2014 no server, no third party. */\n  document.addEventListener('click', function (e) {\n    if (!e.target.closest) return;\n    if (e.target.closest('[data-print]')) { window.print(); return; }\n    if (!e.target.closest('[data-add-calendar]')) return;\n\n    var pad = function (n) { return (n < 10 ? '0' : '') + n; };\n    var stamp = function (v) {\n      var d = v ? new Date(v) : new Date();\n      if (isNaN(d)) d = new Date();\n      return d.getUTCFullYear() + pad(d.getUTCMonth() + 1) + pad(d.getUTCDate()) + 'T' +\n             pad(d.getUTCHours()) + pad(d.getUTCMinutes()) + '00Z';\n    };\n    var lines = [\n      'BEGIN:VCALENDAR', 'VERSION:2.0', 'PRODID:-//RentalForCar//EN', 'BEGIN:VEVENT',\n      'UID:' + CAL_REF + '@rentalforcar.com',\n      'DTSTAMP:' + stamp(),\n      'DTSTART:' + stamp(TRIP.from),\n      'DTEND:' + stamp(TRIP.to),\n      'SUMMARY:Car rental' + (TRIP.vehicle ? ' \u2014 ' + TRIP.vehicle : ''),\n      'LOCATION:' + (TRIP.pickup || ''),\n      'DESCRIPTION:RentalForCar booking ' + CAL_REF + '. Bring the driver licence, the payment card and photo ID.',\n      'END:VEVENT', 'END:VCALENDAR'\n    ].join('\\r\\n');\n\n    var url = URL.createObjectURL(new Blob([lines], { type: 'text/calendar' }));\n    var a = document.createElement('a');\n    a.href = url; a.download = 'rentalforcar-' + CAL_REF + '.ics';\n    document.body.appendChild(a); a.click(); a.remove();\n    setTimeout(function () { URL.revokeObjectURL(url); }, 4000);\n  });\n\n  /* Manage booking told you nothing when you submitted it. */\n  var manageForm = document.querySelector('#route-manage form, [data-route=\"/manage-booking/\"] form');\n  if (manageForm) {\n    manageForm.addEventListener('submit', function (e) {\n      e.preventDefault();\n      var note = document.querySelector('[data-manage-note]');\n      if (!note) return;\n      note.classList.add('is-shown');\n      note.innerHTML = 'We couldn\\'t match that reference and email. Check the reference in your confirmation email, or call <a href=\"tel:+18778516014\" style=\"font-weight:600;text-decoration:underline\">(877) 851-6014</a> and we\\'ll find it for you.';\n    });\n  }\n\n  /* ----------------------------------------------------------\n     LIVE RATES HOOK\n     Static HTML deliberately contains no prices \u2014 nothing that\n     cannot be substantiated is published or indexed. Call this\n     with the supplier API response to fill the rate slots.\n\n       applyLiveRates({ economy: { from: 41, deposit: 25 }, ... });\n     ---------------------------------------------------------- */\n  window.applyLiveRates = function (rates) {\n    var slots = document.querySelectorAll('[data-rate]');\n    for (var i = 0; i < slots.length; i++) {\n      var key = slots[i].getAttribute('data-rate');\n      var r = rates && rates[key];\n      if (!r) continue;\n      slots[i].className = 'price-block';\n      slots[i].innerHTML = '<span class=\"amount\">$' + r.from + '</span>' +\n        '<span class=\"per\">per day</span><br>' +\n        '<span class=\"per\">$' + r.deposit + ' deposit today \u00b7 balance at the counter</span>';\n    }\n  };\n\n  var y = document.querySelector('[data-year]');\n  if (y) y.textContent = new Date().getFullYear();\n\n  if (MULTIPAGE) {\n    try {\n      var saved = JSON.parse(sessionStorage.getItem('rfc-trip') || 'null');\n      if (saved && saved.t) {\n        for (var k in saved.t) if (saved.t[k]) TRIP[k] = saved.t[k];\n        pendingStep = saved.s || 0;\n      }\n    } catch (e) {}\n  }\n\n  /* Initial route render happens last so the observer and widgets exist. */\n  render(currentPath(), false);\n})();\n";
export default function CrScripts() {
  useEffect(() => { try { new Function(JS)(); } catch (e) { console.error(e); } }, []);
  return null;
}
